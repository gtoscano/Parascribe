# Session continuity

When the user says `resume`, read `RESUME.md` completely, inspect the current
worktree, and continue from that handoff. Treat completed benchmarks and paper
edits as finished unless the user explicitly asks to rerun or revise them.
